# Byte-compiled / optimized
**/__pycache__/
*.py[codz]
*$py.class
*.so

# Internal working documents. docs/ is tracked and is the published reference
# set (differentiability, validation, benchmarks, corrections, file index).
# These are narrative development logs: useful locally, not consumer content.
CLAUDE.md
docs/vulcan_jax_notes.md
docs/photo_off_convergence_investigation.md

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg

# Environments
.env
.venv
venv/
.envrc
.direnv/

# IDEs and editors
.idea/
.vscode/
.claude/settings.local.json
*.swp
*.swo
*~

# Type checkers / linters / test caches
.mypy_cache/
.ruff_cache/
.pytype/
.pytest_cache/

# Coverage
.coverage
.coverage.*
htmlcov/
coverage.xml

# Profiling
*.prof
prof/
*.lprof

# JAX / XLA caches and dumps
.jax_cache/
jax_cache/
xla_dump/
*.xla_pb

# Jupyter
.ipynb_checkpoints/

# macOS
.DS_Store

# Scratch / temp files
*.tmp
*.bak
*.orig
*.rej

# VULCAN simulation outputs
output/
*.vul

# FastChem runtime-generated files (regenerated each run by ini_abun._run_fastchem)
src/vulcan_jax/fastchem_vulcan/.fastchem_lock
src/vulcan_jax/fastchem_vulcan/input/parameters.dat
src/vulcan_jax/fastchem_vulcan/input/element_abundances_vulcan.dat
src/vulcan_jax/fastchem_vulcan/input/vulcan_TP/vulcan_TP.dat
src/vulcan_jax/fastchem_vulcan/output/monitor_output.dat
src/vulcan_jax/fastchem_vulcan/output/chem_species.dat
src/vulcan_jax/fastchem_vulcan/output/vulcan_EQ.dat

# FastChem compiled build artifacts (C++ source + makefiles are tracked)
src/vulcan_jax/fastchem_vulcan/obj/*.o
src/vulcan_jax/fastchem_vulcan/obj/*.d
src/vulcan_jax/fastchem_vulcan/fastchem

# Large scientific data formats (typically big binary arrays/tables)
*.h5
*.hdf5
*.nc
*.npy
*.npz
*.parquet
# tests/data/*.npz stays ignored ON PURPOSE — all four regression oracles are
# regenerable from committed scripts (see tests/conftest.py's
# _EXPECTED_FIXTURES for the per-file command), so the ~36 MB of binaries do not
# belong in history. The coverage risk that comes with that — a fresh clone
# silently SKIPPING the tests that compare against them — is closed by the
# collection hook in tests/conftest.py, which FAILS with the regeneration
# command instead of skipping.
*.pkl
*.pickle
*.feather

# ML model checkpoints
*.pt
*.pt2
*.pth
*.ckpt
*.onnx

# Archives
*.zip
*.tar
*.tar.gz
*.tgz

# Logs
*.log
logs/

# Generated plots (the plot/ dir is for generated figures)
plot/*.png
plot/*.pdf
plot/*.jpg
plot/*.jpeg
plot/*.svg

# Generated example figures (examples/output/ is already covered by output/)
examples/plot/

# Local parity-comparison artifacts (figures, scripts, .vul/.npz dumps) vs shami vm_branch / VULCAN-master — not shipped
parity-tests/

# Benchmark / validation artifact bundles. Never shipped: these are local
# evidence sets (runs, .vul dumps, figures, timings). Kept outside the repo.
validation_artifacts/
validation_artifacts_*/
